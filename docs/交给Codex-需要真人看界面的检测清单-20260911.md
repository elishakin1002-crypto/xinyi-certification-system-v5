# 交给 Codex：需要登录界面、真人看/真人点的检测清单

- **日期**：2026-09-11
- **系统**：信义认证系统 V5.0 · 生产 `http://124.223.209.102`（**必须 `http://`，服务器没有 443**）
- **用途**：把这份清单交给 Codex（或任何有多模态能力的 AI）执行

---

## 〇、先看这里：我已经覆盖的，**不要重复做**

下面这些已经有自动化，重复做是浪费：

| 已覆盖 | 方式 | 结果 |
|---|---|---|
| 鉴权（谁能调哪个接口） | 6 角色 × 30 接口 = 168 次真实请求 | 0 处不符 |
| 元素「是否真的被画出来并点得到」 | `elementFromPoint` 包含判定，13 页 × 3 宽度 | 0 处不符 |
| 页面是否有空状态/样例行/帮助/引导/手机端适配 | `npm run checkup` | 14/14 |
| 单元 + 集成回归 | 519 条 | 全过 |
| 数据就绪 | `npm run checkup:data` | 见第六节 |

**我够不到的是「看起来对不对」和「点下去真的能用吗」** —— 这正是要交给你的。

> ⚠️ 一个重要区别：我的可见性检查只回答**「那个坐标上画的是不是它」**，
> 它**完全不能回答**「文字有没有截断 / 有没有重叠 / 对齐乱没乱 / 颜色能不能看清」。
> 这两件事差别很大，别把我的「0 处不符」当成 UI 没问题。

---

## 一、账号

生产上这些账号存在（**密码问我要，不在文档里**）：

| 岗位 | 账号 | 说明 |
|---|---|---|
| 系统管理员 | `admin` | 金恩来 |
| 总经理 | `zengyunjun` | 曾云俊 |
| 总助 | `lizhiwei` | 李智薇 |
| 食品认证顾问 | `huangjiajia` | 黄佳佳 |
| 食品包装顾问 | `shangchunzi` | 商春姿 |
| 体系认证顾问 | `huangbangyu` | 黄邦煜 |
| 财务 | `jinxiaoyan` | 金小雁 |
| 预览账号 | `yl-zongjl` / `yl-zongzhu` / `yl-guwen` / `yl-caiwu` | 各角色预览 |

**⚠️ 连错 5 次锁 15 分钟**，先把密码准备好再输。

---

## 二、优先级 P0：不做完不能上线

### P0-1　图表有没有真的画出来

三处用了 recharts：`pages/Dashboard.tsx`、`pages/Audit.tsx`、`components/MonthlyReview.tsx`

**为什么必须人看**：图表是 canvas/svg 渲染，我的 DOM 检查只能看到容器存在，
**看不出里面是空白、坐标轴标签叠在一起、还是数据画错了**。

逐项确认：
- [ ] 工作台「经营趋势」折线/柱状图 —— 有没有画出来；X 轴 7 个月份标签**有没有重叠**
- [ ] **数据为 0 时**图表长什么样 —— 是空白一片，还是有「暂无数据」？
      （生产现在 24 个项目全部已完成、0 个在做，很多图会是空的）
- [ ] 不符合项页的趋势图、类型分布图
- [ ] 月度复盘 `MonthlyReview` 的图
- [ ] **手机宽度下**图表是横向溢出、还是缩成一团看不清

### P0-2　文件上传 —— 8 个入口，一个都没真传过文件

| 位置 | 传什么 |
|---|---|
| `pages/Contracts.tsx:1047 / 1607` | 合同附件、合同扫描件（走 AI 识别） |
| `pages/Audit.tsx:1799` | 不符合项整改证据（现场照片） |
| `pages/Customers.tsx:1518 / 1796` | 客户证书、资料 |
| `pages/Leads.tsx:408` | 线索批量导入（Excel） |
| `pages/Finance.tsx:506` | 回款凭证 |
| `components/IngestionUploader.tsx:96` | 知识中心文档 |

每个入口都要：
- [ ] 传一张**手机拍的大图（3-5MB）** —— 系统会压缩（实测 3.21MB → 358KB），确认**压缩后还看得清**
- [ ] 传一个 **PDF**、一个 **Word**、一个 **Excel**
- [ ] 传一个**超大文件（>20MB）** —— 看有没有友好提示，还是直接卡死/报错
- [ ] 传一个**错误格式**（比如 .exe 改名成 .pdf）—— 看提示说不说人话
- [ ] 上传后**刷新页面**，确认文件还在、点得开

> 已知相关坑：图片会剥 EXIF/GPS、按 EXIF 摆正方向。**确认竖拍的照片没有躺倒。**

### P0-3　导出功能 —— 导出的文件长什么样

导出入口在：`pages/Contracts.tsx`、`pages/Audit.tsx`、`pages/Knowledge.tsx`

- [ ] 每个导出点一遍，**打开导出的 Excel** 看：中文乱码没有？列宽？金额是不是「元」（库里存的是「分」）？
- [ ] 数据为空时导出 —— 会不会报错

> **金额是最高危的一项**：库里 `project_amount = 800000` 表示 8000 元。
> 导出如果没除 100，账目就全错了。**必须拿计算器核一笔。**

### P0-4　AI 功能的输出质量

AI 入口在 7 个页面。我只能验「接口通没通」，**验不了「AI 说得对不对」**。

- [ ] **合同 AI 识别**（`pages/Contracts.tsx`）：传一份真实合同扫描件，
      看识别出的甲方/乙方/金额/日期**对不对**。错了比没有更危险 —— 人会直接采信
- [ ] **文档摘要**（知识中心）：对一份长文档生成摘要，看是不是废话
- [ ] **AI 助手对话框**（右下角）：问几个业务问题，看答得对不对、会不会瞎编
- [ ] **不符合项的「同类问题推荐」**：填一条问题描述，看推荐的是否相关

> 已知设计：正文少于 200 字的文档**拒绝生成摘要**（不让模型照标题编）。确认这条真的生效。

---

## 三、优先级 P1：影响第一印象

### P1-1　长文本会不会撑破布局

生产库里最长的项目名 **47 个字**：

```
【情报跟进】平阳温州康田包装有限公司：塑编塑包/食品级包装企业资料核验与认证/食品包装合规跟进
```

- [ ] 项目列表里这一条 —— 是省略号，还是把整行撑开/把右边的按钮挤出屏幕
- [ ] 手机宽度下同上
- [ ] 建一个**名字很长的客户**（30 字以上），看客户列表、项目详情、合同页引用它的地方
- [ ] 不符合项的「问题描述」填 500 字，看详情页和列表

### P1-2　新手引导的实际体验

- [ ] 每个角色各走一遍完整引导（6 步）
- [ ] **浮层有没有挡住它要指的那个元素**
- [ ] 引导指向的元素**在手机上存不存在**（有些元素只在桌面有）
- [ ] 走到一半刷新页面 —— 会不会卡在中间出不来
- [ ] 「跳过」之后还会不会反复弹出

> 已知坑：引导卡片高度曾经写死 260px，实际三四百像素，**「下一步」被挤出屏幕，卡死在第 3 步**。

### P1-3　三层帮助答不答得上来

右上角问号 → 三层：① 认识我的工作台 ② 了解当前模块 ③ 解释这一项

- [ ] 在**每个页面**点「了解当前模块」—— 内容对不对得上这一页
- [ ] 打开一个弹窗（比如建项目），点帮助 —— 它讲的是**这个弹窗**还是整页
- [ ] 用「解释这一项」点十来个按钮 —— **有没有答不上来的**

> **答不上来本身就是缺陷。** 帮助答不上来比功能有 bug 更容易劝退人。

### P1-4　空状态说不说人话

生产现在很多地方是空的（0 个在做的项目、455 条线索没跟进人）。

- [ ] 每个页面、每个筛选组合下的空状态，是**一片空白**还是**说清了为什么空 + 下一步去哪**
- [ ] 特别看：我的任务、项目管理（切各种筛选）、不符合项、知识中心

---

## 四、优先级 P2：真机与兼容

### P2-1　真手机实测（模拟器测不出来的）

- [ ] **软键盘弹出时**，输入框会不会被键盘挡住（尤其是弹窗里靠下的字段）
- [ ] iOS Safari 上的表现（和 Chrome 有差异）
- [ ] 横屏
- [ ] 长列表（455 条线索）滚动**卡不卡**
- [ ] 触摸目标够不够大（按钮是不是太小点不准）

### P2-2　浏览器兼容

- [ ] Chrome / Edge / Safari / 微信内置浏览器
- [ ] **微信内置浏览器最重要** —— 同事很可能从微信点链接进来

---

## 五、优先级 P3：多人并发

- [ ] 两个人同时改**同一个项目** —— 后保存的会不会盖掉先保存的
- [ ] 一个人在 A 页面改，另一个人在 B 页面看同一条数据 —— 刷新后是否一致

> **这是已知的未修问题**（待办 P0-19b）：state store 是整份数组写入，
> 两人同改后写的盖先写的。**请确认实际影响有多大**，决定要不要上线前修。

---

## 六、执行时请一并留意的「已知高危点」

这些是这个项目**真实踩过的坑**，改动后特别容易复发：

| 风险 | 怎么看 |
|---|---|
| **工时单位** | 工作日志填 **0.5 小时**，保存后回看还是不是 0.5（历史上存成过 50） |
| **金额单位** | 任何显示金额的地方，核一笔真实数据（库里存「分」） |
| **筛选把新建的东西藏起来** | 新建项目/线索后，**它在不在列表里**（踩过三次） |
| **两处实现不一致** | 同一个数字在工作台和列表页**对不对得上** |
| **手机端和桌面端两套 DOM** | 同一个功能在两边都要试 |

---

## 七、报告格式

每发现一个问题，请给：

1. **页面 + 角色 + 屏幕宽度**（三者缺一不可，很多问题只在特定组合出现）
2. **截图**
3. **重现步骤**
4. **你预期看到什么**

分级：**阻断**（干不了活）/ **严重**（有替代路径但很难受，或数字不对）/ **一般**（界面别扭）/ **建议**（新功能——记录不做）

> 最后一条是测试期纪律：真实业务一跑，需求会像雪片一样来，全接就永远上不了线。

---

## 八、请**不要**做的事

- ❌ 不要重复做第〇节列的自动化检查
- ❌ 不要删任何数据（「谁做的」审计链不能断）
- ❌ 不要改 `components/OnboardingTour.tsx` / `SampleRow.tsx` / `src/modules/onboarding/*`（另有人在改）
- ❌ 不要把密码写进任何文件或对话
- ❌ 发现问题**先报告再改** —— 生产在跑，改错了有人当天干不了活

---

## 一句话

我能验的是「**接口放不放行**」和「**元素有没有被画出来**」；
你要验的是「**看起来对不对**」和「**点下去真的能用吗**」——
尤其是**图表、上传、导出、AI 输出质量**这四块，我结构上就够不到。
