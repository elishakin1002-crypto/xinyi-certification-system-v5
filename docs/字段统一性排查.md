# 字段统一性排查

**同一概念不同说法：12 条；同一个词指不同东西：6 条；词不达意：19 条。** 共 37 条，按概念或问题组计数，不按出现次数计数；存疑及已统一项不计入。

基准：`c94bb3d`，2026-09-16。开始时已执行 `git log --oneline -5` 确认。扫描 `pages/`（24 个）、`components/`（21 个）、`src/ui/`（2 个）共 **47 个 TSX 文件、26,290 行**，包括子目录、手机分支、弹窗、悬浮说明、导出表头及传入公共组件的文案。对动态卡片追查了取数服务；没有开浏览器、启动服务或修改业务代码。

**下文文件:行号均指 c94bb3d 的源码**，可用该提交核对。排查中出现他人对 `services/dashboardMetrics.ts`、`server/generated/dashboardMetrics.cjs` 的并行修改，未触碰；其中“项目延误率”已另记在“已经统一的”，不计入问题数。本文是静态代码证据，不代表已核对线上数据。

只把读同一字段、同一枚举或同一判据的地方归为同一概念。不同对象合理共用“状态”“名称”等表头，不一概当错；第二类列的是值得加限定词、否则容易混淆的口径。用户指定已修的项目四卡、财务应收状态、客户总数等不重复报。

## 一、同一个概念，不同地方用了不同的词（12 条）

### A01 不符合项状态：中文、英文、手机简化成另一状态

- 不符合项桌面：`pages/Audit.tsx:1197`，`issue.status` 的 Open / Rectifying / Verifying / Closed 显示“待整改 / 整改中 / 待验证 / 已关闭”；编辑选择器 `pages/Audit.tsx:1664` 同口径。
- 同页手机 `pages/Audit.tsx:1489`，除 Closed 外一律显示“整改中”；Open 和 Verifying 因此被改了含义。列表筛选 `pages/Audit.tsx:1451` 又把 Closed 叫“已归档”，实际只判断 Closed（`pages/Audit.tsx:385`）。
- 客户详情 `pages/Customers.tsx:1445`、项目详情 `pages/Projects.tsx:2144`、知识卡 `pages/Knowledge.tsx:611`、知识预览 `pages/Knowledge.tsx:866`、重复问题提示 `pages/Audit.tsx:1732`、验证面板 `pages/Audit.tsx:1874`，直接显示同一状态的英文值。
- **建议：统一四态为“待整改 / 整改中 / 待验证 / 已关闭”**，筛选“未关闭 / 已关闭”。四个阶段确实不同，不能在手机端合并，也不能把关闭说成归档。

### A02 不符合项严重度：严重、重大、英文值并存

- `pages/Audit.tsx:1184`、`pages/Audit.tsx:1656`：同一 `severity` 的 Major / Minor / Observation 显示“严重 / 一般 / 观察”，表单显示“严重不符合 / 一般不符合 / 观察项”。
- `pages/Audit.tsx:1414`、`pages/Customers.tsx:1419`、`pages/Projects.tsx:2118`：Major 又叫“重大”。客户与项目明细 `pages/Customers.tsx:1444`、`pages/Projects.tsx:2143`，知识预览 `pages/Knowledge.tsx:870` 直接显示英文值。
- **建议：统一“严重不符合项 / 一般不符合项 / 观察项”**，窄徽章可缩成“严重 / 一般 / 观察”。Major 已有明确中文选项，“重大”不另起一档；统计未关闭项时补“未关闭”。

### A03 不符合项截止日：死线、期限、截止

- `pages/Audit.tsx:1673` 表单“整改死线”写 `formData.deadline`；`pages/Audit.tsx:1489` 手机“期限”、`pages/Audit.tsx:1527` 桌面“整改截止”读 `issue.deadline`。
- `pages/Customers.tsx:1452`、`pages/Projects.tsx:2150` 也读同一不符合项的 `deadline`。
- **建议：统一“整改截止日期”**；手机可用“整改截止”。它是约定日期，不需要“死线”这种额外语气。

### A04 知识分类：上传时和查看时不是同一名称

- `pages/Knowledge.tsx:129` 的分类映射，供页签（`:521`）、卡片（`:602`）、预览（`:812`）使用：Product Service＝“产品资料”，Standard＝“标准法规”，Training＝“培训资料”。
- 上传选择器 `pages/Knowledge.tsx:688`、`:689`、`:691` 对同一 `category` 值分别叫“产品手册 / 行业标准 / 内部培训”。
- **建议：统一“产品资料 / 标准法规 / 培训资料”**。这组覆盖面更完整；上传后分类名称不会突然变了。

### A05 企业信用代码

- `pages/Leads.tsx:769`：“统一信用代码”，读写 `editingLeadData.unifiedSocialCreditCode`（`:773`）。
- `pages/Customers.tsx:1879`：“信用代码”，表单字段仍是 `unifiedSocialCreditCode`；去重提示 `pages/Customers.tsx:696` 则称“统一社会信用代码”。
- **建议：统一“统一社会信用代码”**。同一企业标识已经用于去重，用全称才能清楚区分普通内部编号。

### A06 线索联系电话

- `pages/Leads.tsx:1009` 新建叫“手机号”，下一行写 `newLead.mobile`；`:730` 编辑叫“联系电话”，`:735` 写 `editingLeadData.mobile`。
- **建议：统一“联系电话（手机或座机）”**，与编辑页允许普通联系电话的现状一致。如果业务只收手机，应先确立校验规则，不能仅凭字段名 mobile 推断。

### A07 第三方任务分类

- 项目任务徽章 `pages/Projects.tsx:2311`、`:2423`、`:2495`：`task.category === 'ThirdParty'` 显示“合作”。
- 同页模板编辑 `pages/Projects.tsx:3722`：同一任务分类值 ThirdParty 显示“第三方”。
- **建议：统一“第三方”或“第三方任务”**。这里标的是任务交由谁执行，“合作”还被服务交付方式使用，含义更宽。

### A08 聚合提醒的严重程度

- 顶栏铃铛 `components/Layout.tsx:516`：聚合提醒 severity 的 high / medium / low 显示“紧急 / 关注 / 一般”，在 `:584` 使用。
- 工作台风险面板 `pages/Dashboard.tsx:904`：同一聚合提醒 severity 显示“严重 / 高 / 中低”，由 `pages/dashboard/RiskPanel.tsx:101` 渲染。
- 工作台聚合列表 `pages/Dashboard.tsx:1582` 又是“严重风险 / 风险提示 / 机会/到期”。
- **建议：严重程度统一“紧急 / 关注 / 一般”**；风险、机会、到期另显示类型。尤其不能让 medium 在另一处变成“高”。

### A09 用户反馈的类别和紧急程度

- 提交端 `components/FeedbackModal.tsx:30`：kind 的 bug / confused / wrong / improve 显示“系统出错了 / 我不知道怎么操作 / 能用，但结果不对 / 希望这样改”。
- 管理端 `pages/dashboard/SysAdminBoard.tsx:45`：相同值显示“出错了 / 不会用 / 结果不对 / 希望改进”。severity 的 annoying 在提交端 `components/FeedbackModal.tsx:38` 是“能绕过去，但烦”，管理端 `pages/dashboard/SysAdminBoard.tsx:48` 变成“有点烦”。
- **建议：两端共用“系统出错 / 操作不清楚 / 结果不对 / 改进建议”，紧急程度保留“挡住干活 / 能绕过但影响使用 / 不急”**。管理员看到的应当是用户选中的含义，“有点烦”弱化了影响。

### A10 账号审计动作：桌面中文、手机操作码

- `pages/AuthAuditLogs.tsx:12` 定义 USER_CREATE＝“创建员工”等动作名；桌面 `:190` 使用 `actionLabels[log.action]`。
- 手机 `pages/AuthAuditLogs.tsx:156` 直接输出 `log.action`，同一条记录显示 USER_CREATE 等代码。
- **建议：手机也使用现有中文动作映射**。操作码可留详情，日常列表不应要求人理解内部代码。

### A11 情报紧急度：列表中文、详情英文

- `pages/IntelRadar.tsx:906`：`s.urgency` 显示“高紧急 / 中紧急 / 低紧急”。详情 `:1007` 直接输出同一记录的 `selected.urgency`（high / medium / low）。
- **建议：统一“紧急度：高 / 中 / 低”**。保留该字段原有三级，不把它和提醒的 severity 强行合并。

### A12 合同回款节点：支付、款项、收款、回款

- `pages/Contracts.tsx:2090` 编辑 `receivables[].node` 的区域叫“支付节点与金额”，按钮叫“添加款项节点”；同页 `:1494` 查看相同数组叫“回款计划”。
- `pages/Finance.tsx:472` 表头叫“款项节点（摘要）”，读 `r.node`；导出表头 `:192` 叫“收款节点”。项目详情 `pages/Projects.tsx:1706`、`:1810` 称“回款明细 / 回款节点”。
- **建议：统一“回款节点”，所属区域叫“回款计划”，金额叫“本期应收金额”**。这些都是公司要收的钱，能与财务的支出结算分清。

## 二、同一个词，实际指不同东西（6 条）

### B01 “负责人”：项目负责人和任务执行人

- 项目卡 `pages/Projects.tsx:2935`、桌面表头 `:2973` 及详情 `:1289`：“负责人”读 `project.manager`。
- 逾期任务清单 `pages/Projects.tsx:2891`：“负责人”读 `task.owner || project.manager`。
- 新建项目 `pages/Projects.tsx:3312` 又叫“执行负责人”，写 `formData.manager`；合同立项 `pages/Contracts.tsx:1677` 叫“项目负责人”，通过 `:420` 所在的创建参数写入项目。
- **建议：项目归口统一“项目负责人”，任务统一“任务执行人”**；任务未指定而回退项目负责人时注明“由项目负责人承接”。项目经理和具体干活的人可以不是同一人。

### B02 “已确认”：项目金额已确认和结算单已确认

- `pages/Projects.tsx:1556`、`:3557`：`project.costStatus === '已确认'`，指项目金额确认，涉及结项前置条件。
- `pages/Finance.tsx:623`、`:678`：`settlement.status === 'confirmed'`，指结算审核完成，但尚可点击“标记支付”（`:688`）。
- **建议：分别叫“金额已确认”“结算已确认（待支付）”**。不能让确认了金额看起来等于批准付款，更不能等于已支付。

### B03 “已归档”：退出活跃列表、已存入知识库、问题关闭

- 合同 `pages/Contracts.tsx:1418` 读 `contract.archiveStatus`；`:371` 明确归档后移入历史库。
- 模板 `pages/Projects.tsx:3760` 读 `tpl.archived`，`:3632` 控制是否显示归档模板。
- 客户证书档案 `pages/Customers.tsx:1714`：对已找到的知识文档显示“已归档”，不是合同/模板那种退出活跃列表的状态。
- 不符合项筛选 `pages/Audit.tsx:1451`：“已归档”实际只是 `status === Closed`（`:385`）；这一处的映射修正见 A01，不另计第三类。
- **建议：合同、模板保留“已归档”；知识文档叫“已存入知识库”；不符合项叫“已关闭”**。保存资料、停止活跃使用、完成整改是三件事。

### B04 “回款率”：分母分别为合同总额、应收计划总额

- 合同卡 `pages/Contracts.tsx:1317`：`:1165`—`:1175` 算未归档且符合范围的已收节点金额 ÷ 合同金额合计。
- 财务卡 `pages/Finance.tsx:424`：`:225`—`:228` 算全部合同的已收节点金额 ÷ 全部回款节点金额合计。
- 工作台趋势 `pages/Dashboard.tsx:681`：`:645`—`:668` 的近七个月已收曲线合计 ÷ 新签合同金额合计，时间范围又不同。
- **建议：分别标“合同金额回款率”“应收计划回款率”“近七个月应收实收/同期签约金额比”**，并显示范围。更理想的是先决定经营上需要哪一项再统一算法；仅统一名称会掩盖分母差异。

### B05 “本周”：自然周、回看七天、向前七天

- 项目日志 `pages/Projects.tsx:2534`、`:2538`：“本周日志 / 本周工时”，`:1233` 从本周一零点起算。
- 顾问工作台 `pages/dashboard/ConsultantDashboard.tsx:57` 接 `metrics.bottomCards`；`services/dashboardMetrics.ts:607`—`:611`、`:642`—`:644` 的“本周日志条数 / 本周工时统计”从当前时间减六天起算。老板、总助日志覆盖率 `services/dashboardMetrics.ts:276`、`:379` 也用回看窗口。
- 我的任务 `pages/MyTasks.tsx:243`、`:290`：“本周内”，`:122`—`:132` 按今天起七天分桶，并非本周日截止。工作台 `components/MyWorkWidget.tsx:82`—`:87`、`:126`、`:170` 同样把未来七天叫“这一周 / 这周”。
- **建议：自然周保留“本周”，滚动回看改“近七天”，向前看改“未来七天”**；若要真的统一为自然周，需要一起改边界，不能只换字。过去和将来的窗口也不能混成一个“周”。

### B06 “待支付”：列表只指草稿，卡片包含已确认

- `pages/Finance.tsx:623` 筛选、`:679` 徽章、`:314` 手机状态：只有 `status === draft` 叫“待支付”。confirmed 另叫“已确认”。
- 同页 `pages/Finance.tsx:611`：“待支付 / 草稿”金额来自 `:239` 的 `status !== paid`，包括 draft 和 confirmed。
- **建议：draft 统一“待确认结算”，confirmed 叫“待支付”，汇总叫“未支付结算金额（含待确认）”**。能直观看出还有一道确认步骤，也能解释卡片为何大于草稿筛选金额。

## 三、词不达意：名字与数字、动作或事实不符（19 条）

### C01 “未开票项目”数的是结算草稿

- 财务工作台 `pages/dashboard/FinanceDashboard.tsx:22`—`:25` 渲染异常卡；`services/dashboardMetrics.ts:715` 名为“未开票项目”。
- 实际 `services/dashboardMetrics.ts:672` 只数 `settlements` 中 `status === draft`，既没判断发票，也没按项目去重。
- **建议：当前数据下改成“待确认结算单”并进入相应结算筛选**。如确实要做未开票指标，必须有发票字段和项目判据，不能用草稿替代。

### C02 “预估金额”显示固定 10%

- 项目结算区域 `pages/Projects.tsx:1698`—`:1701`：“结算对象”读 `project.manager`，“规则类型”为固定“按回款比例提成”，“预估金额”直接写死 `10%`。
- **建议：至少把标签改成“参考提成比例”并标“尚未核算”**；真实预估金额必须读取约定规则并计算元数。10% 是比例，且目前没有读取该人的提成规则。
- 同段 `:1701` 仍说“点击发起结算即可生成应付单”；当前入口 `:1639`—`:1642` 已改为去 Excel 结算导入。应同步说明为“前往结算页导入结算单”。这里报的是遗留说明与固定比例，不重复报告已修的死按钮。

### C03 “已支付结算（YTD）”没有本年限制

- `pages/Finance.tsx:604` 的 YTD 表示本年截至当前；取数 `:229`—`:238` 只按类型、状态、搜索筛选后合计 paid，没有年份条件。
- **建议：改“当前筛选已支付金额”**，或真的加本年支付日期过滤后再用“本年已支付结算”。跨年记录当前也会计入。

### C04 “本月已收”和回款趋势用的是约定到期日

- 财务工作台 `pages/dashboard/FinanceDashboard.tsx:13`、`services/dashboardMetrics.ts:708`：“本月已收”；`:663` 实际按 `status === paid` 且 `dueDate` 在本月合计。
- 工作台“已回款”趋势 `pages/Dashboard.tsx:1275`，在 `:595`—`:604` 把 paid 节点按 `dueDate` 分月；财务月筛选 `pages/Finance.tsx:455` 对应的过滤在 `:216` 也读 `dueDate`。
- **建议：现有算法应叫“本月到期应收中的已收金额”，趋势注明“按应收到期月”**。要叫本月实际回款，应按真实到账日期归月；例如八月到期、九月才收到，现在仍记在八月。

### C05 “本月营收项目金额”可能变成新签合同金额

- 老板卡 `pages/Dashboard.tsx:727`—`:731`：`revenueAmountThisMonth > 0` 才显示营收项目金额，否则用 `monthContractAmount`，点击也从项目切到合同。
- 该卡实际由 `pages/Dashboard.tsx:1197` 传给老板面板；不是未使用的旧卡。`pages/dashboard/BossDashboard.tsx:15` 的说明却说“缺失时回退合同金额”，容易误解为逐项目补金额。
- **建议：营收项目金额为零就显示零，缺失就明确待补；新签合同金额另列**。没有营收项目不等于可以拿尚未交付的签约额充数。

### C06 “已转化为线索”实际完成的是转研判项目

- `pages/IntelRadar.tsx:762` 卡片读 `stats.converted`；`:351` 仅数 `signal.status === converted`。
- `pages/IntelRadar.tsx:1063`—`:1071` 动作为 `convertSignalToFollowUpProject`，详情 `:1100` 显示 `convertedTo.projectId`；`context/AppContext.tsx:1743`—`:1760` 确认创建的是跟进项目并把情报标为 converted。
- 项目里另有“转为线索”步骤（`pages/Projects.tsx:1489`—`:1498`）。
- **建议：卡片叫“已转为研判任务”**。创建项目不等于已建销售线索，后续转线索可另计。

### C07 “任务已全部完成”也会出现在零任务或全跳过项目

- 项目详情 `pages/Projects.tsx:1293`—`:1294`：只要 `getNextTask(project)` 为空就显示“任务已全部完成”。
- `pages/Projects.tsx:847`—`:850` 只查未完成任务；零任务、全部 Completed、全部 Skipped 都可能没有下一条。
- **建议：与列表 `pages/Projects.tsx:3025`—`:3026` 一致，分“尚未安排任务 / 无待办任务（含已跳过） / 所有任务已完成”**。没安排和决定不做都不是做完。

### C08 任务 Pending、Skipped 被标成“进行中”

- 项目关联整改任务 `pages/Projects.tsx:2145`，日志任务选择器 `:2583`：仅区分 Completed 与其他，其他全部显示“进行中”。
- 实际公共控件 `components/TaskStatusControl.tsx:51`、`:58`—`:60` 有 Pending / InProgress 两态；`components/TaskSkipButton.tsx:27` 还明确有 Skipped。
- **建议：按实际四态显示“待开始 / 进行中 / 已完成 / 已跳过”**。准备做、正在做和不做了，是三种不同进度信号。

### C09 没有监管计划也显示“已全部完成”

- 客户证书详情 `pages/Customers.tsx:1557`—`:1558`：空 `auditPlan` 得到空数组和空 `nextAuditNode`。
- `pages/Customers.tsx:1650` 将没有 nextAuditNode 一律显示“已全部完成”；同卡下方 `:1698` 却会显示“暂无监管计划”。
- **建议：先判断有没有计划，空数组显示“尚未生成监管计划”**；有计划且都完成才说全部完成。

### C10 “最近项目（Verified）”可能只是最近签的一份合同

- 客户 PDCA 卡 `pages/Customers.tsx:1217` 叫“最近项目（Verified）”。`:403`—`:415` 中 `lastProjectType / lastProjectAt` 缺失时，回退到 `latestContract.serviceLine / title / signDate`。
- **建议：有真实项目记录时叫“最近项目”；回退时叫“最近签约服务 / 签订日期”，去掉 Verified**。签合同本身并不证明该服务已交付、通过验证或完成项目。

### C11 “AI 已学习”“AI 训练数据”实际只是知识访问许可

- 知识卡 `pages/Knowledge.tsx:581`—`:582`：只判断 `doc.aiVisible` 就提示“AI 已学习（可引用）”；统计 `:127` 也只数许可已开的文档，正文是否存在不是条件。
- `pages/AICenter.tsx:84`—`:92` 的“管理 AI 训练数据”实际跳知识中心的 ai_ready 筛选；知识上传 `pages/Knowledge.tsx:748` 还叫“允许 AI 读取并学习（RAG）”。
- **建议：统一“允许 AI 引用 / AI 可引用资料”**。许可开启并非已读取、已建好索引或已训练模型；知识页卡片 `:470` 的“AI 权限已开文档”已经更接近事实。

### C12 “运行中 / 就绪 / 已激活”是固定文案，不是健康检查

- `pages/AICenter.tsx:104` 叫“系统健康度与模型状态”，`:116` 固定显示数据保护“已激活”，`:127` 固定显示合同分析模型“运行中”，`:138` 固定显示语音引擎“就绪”。
- 这些标签旁没有读取探测结果或运行状态；与下方已明确说明未接后端的规划开关（`:158`）不同，上方看起来是在报告当前事实。
- **建议：没有检查结果时显示“未检测”，区域改“能力说明”**。若保留健康度标题，就应读取实际可用性和检查时间；固定绿点不能证明服务正在运行。

### C13 “只看在职”实际只排除停用账号

- `pages/Employees.tsx:329` 的“只看在职”切换 `showDisabled`；`:115`—`:116` 仅按 `user.status !== disabled` 过滤。
- **建议：改“只看启用账号”**。账号启用状态与是否在职不是同一个字段，页面还明确允许兼职、临时合作方（`:612`）。

### C14 “还没首次登录的账号”实际数需改密码账号

- 系统管理员卡 `pages/dashboard/SysAdminBoard.tsx:179`—`:180` 读 `accounts.pending_password`。
- 来源 `server/routes/sysadminOverview.js:119` 数 `must_change_password`，并未检查首次登录。重置密码也会设此标志（`server/authStore.js:909`）。员工页 `pages/Employees.tsx:439` 对同一含义已叫“需改密”。
- **建议：改“待修改密码账号”**。已登录多次但刚重置过密码的人也会被算入。

### C15 “即将成交客户 / 今日必须联系客户 / 待报价客户”没有相应判据

- 销售看板 `pages/dashboard/SalesDashboard.tsx:18`、`:30` 渲染机会、今日联系卡；实际标题在 `services/dashboardMetrics.ts:539`、`:546`、`:547`。
- `services/dashboardMetrics.ts:498` 的 `hotLeads` 只是 High 意向、尚未转化的线索，且先截前五条；同一数组既叫“即将成交”，又叫“今日必须联系”。没有今日约定联系日期，也不证明即将成交，超过五条仍显示五。
- “待报价客户”只筛 `lead.status === Pending`，而统一线索状态 `src/ui/statusBadge.tsx:27` 明确定义为“跟进中”，没有报价判据。
- **建议：分别叫“高意向未转化线索（展示前五条）”“优先联系线索”“跟进中线索”**；数量卡应统计全量，展示列表才截断。确需今日、成交或报价指标时补对应业务条件。

### C16 “合同金额缺失项目”查的是项目金额

- 财务异常卡 `services/dashboardMetrics.ts:713`、项目焦点说明 `pages/Projects.tsx:458` 都叫“合同金额缺失项目”。
- 判据分别是 `services/dashboardMetrics.ts:670`、`pages/Projects.tsx:414`：Delivery 且 `project.projectAmount <= 0`，不是关联 `contract.amount` 缺失。项目详情甚至允许从有金额的合同带入（`pages/Projects.tsx:1581`—`:1590`）。
- **建议：改“待补项目金额”**。合同金额可能已经齐全，只是项目额没填；两个字段不能混叫。

### C17 “严重度评分”实际是等级选择

- `pages/Audit.tsx:1656` 表单标签为“严重度评分”；`:1658`—`:1660` 实际写 `severity` 的 Minor / Major / Observation，没有分值。
- **建议：改“严重程度”**，选项按 A02。等级分类不是打分，避免和同页相似问题“匹配度”的数值混淆。

### C18 “本月待验证”实际是本月发现且目前仍待验证

- 不符合项“本月走势” `pages/Audit.tsx:550`—`:552` 写“当前月……待验证 X 项”；图例 `:1302` 也仅写“待验证”。
- `pages/Audit.tsx:452` 将当前 status 为 Verifying 的记录按 `createDate` 所在月计数，不是按进入待验证的日期，也不是本月末状态快照。
- **建议：保留当前算法就标“按发现月份统计的当前待验证项”**，本月说明写“本月发现、当前仍待验证”。真正的月末库存趋势需要状态历史，不能拿当前状态倒填。

### C19 同一合同的中风险在手机上变成“正常”

- 桌面 `pages/Contracts.tsx:1418`：`riskLevel === Medium` 显示“中风险”，Low 才显示“正常”。
- 手机 `pages/Contracts.tsx:1608`：仅 High 显示“高风险”，其他一律“正常”，同一条 Medium 合同在手机端丢掉风险提示。
- **建议：两端共用 High / Medium / Low →“高风险 / 中风险 / 低风险”的完整映射，缺值显示“未评估”**。这里只核对合同字段；不把客户 riskStatus 不同业务算法硬归为同一风险。

## 四、已经统一的（不计问题）

| 已核对的概念 | 证据与结论 |
| --- | --- |
| 项目四张卡 | `src/modules/glossary.ts:43` 起定义项目口径；`pages/Projects.tsx:2764`—`:2797` 引用四项术语。保留既有修复，不再报任务数冒充项目数。 |
| 财务应收五类显示文案 | `pages/Finance.tsx:94` 定义 receivableStatusLabel；导出 `:200`、桌面 `:514`、手机 `:581` 共用；筛选 `:447` 是待回款 / 已逾期 / 已到账。不再报此前桌面/手机两套状态的问题。 |
| 客户总数和合同汇总修复 | `pages/Customers.tsx:964` 的客户总数；`:971`、`:985` 的合同数、金额追到本页即时汇总，不再把此前冗余字段问题计入。 |
| 线索状态 | `pages/Leads.tsx:472` 的筛选调用 getStatusBadge，实际复用 `src/ui/statusBadge.tsx:25` 的 lead 映射：新增 / 跟进中 / 已转化 / 高风险 / 已丢失。销售指标另造“待报价”的问题已单列 C15。 |
| 任务完成、开始、跳过操作 | `components/TaskStatusControl.tsx:49`、`:56` 与 `components/TaskSkipButton.tsx:27`、`:79` 分别承接实际状态变化；`pages/MyTasks.tsx:260`—`:262` 明确未完成与全部的区别。没有把任务“已完成”误当项目结项用词问题。 |
| 未完成、逾期的基础定义 | `src/modules/glossary.ts:105` 起有未完成任务、逾期任务口径，明确跳过不算未完成；财务 `pages/Finance.tsx:119` 按 isReceivableOverdue 判断应收逾期。未将注释中的淘汰术语当成界面问题。 |
| 员工正式角色名 | `pages/Employees.tsx:376`、`pages/AICenter.tsx:274` 都读 SYSTEM_ROLES；`components/OnboardingTour.tsx:46`—`:47` 的六角色与其对应。角色权限和岗位标签各有自己的字段，没有要求混成一项。 |
| 跟进方式 | `pages/Leads.tsx:222` 与 `pages/Customers.tsx:280` 都把 call / visit / wechat / email / system 显示成电话 / 拜访 / 微信 / 邮件 / 系统；图标差异不算术语差异。 |
| 登录与密码 | `pages/Login.tsx:100`、`:114`，`pages/ChangePassword.tsx:129`、`:143`、`:154`，`components/LoginSessions.tsx:160`：账号、密码、当前密码、新密码、确认新密码、我的登录设备分工清楚。 |
| 样例与真实记录 | `components/SampleRow.tsx:69`、`:110`、`:134` 和 `pages/Strategy.tsx:69` 都明确“样例 · 不是真实数据”；样例内容不计真实字段冲突。 |
| 已修按钮 | `pages/Finance.tsx:461` 已接 exportReceivables；`pages/Projects.tsx:1639`—`:1642` 已明确结算从 Excel 导入。不重复报死按钮，C02 只报同区域尚未同步的说明和比例标签。 |
| 并行修复：项目延误率 | c94bb3d 的 `services/dashboardMetrics.ts:334` 原为逾期任务 / 未完成任务；本轮只读期间他人工作区改为有逾期任务的进行中项目 / 进行中项目。未将它计入待修 19 条；这是观察到代码修复，未验证部署或运行。 |

## 五、存疑（不计问题）

### D01 累计合作金额、累计消费、累计产值应不应该是一个指标

- `pages/Customers.tsx:985` 是合同汇总的“累计合作金额”；`:1209` 的“累计消费”来自 `:405`—`:406`，取客户 `totalAmount` 与已收金额的较大值。
- `pages/Projects.tsx:3559` 的费用补录提示说金额将计入“累计产值”。三者涉及合同额、存量累计字段、到账额，不能认定是同一个概念。
- **待确认：业务要看签约累计、交付产值还是实收累计，以及历史 totalAmount 的口径。** 建议分别明确“累计签约金额 / 累计交付产值 / 累计实收金额”，不直接全部改成“累计金额”。

### D02 “本周完成任务数”数自动日志，能否一条日志代表一个任务

- 顾问顶部卡 `services/dashboardMetrics.ts:633`，取数 `:610` 只数 `myWeekLogs.source === task_transition`；`pages/Projects.tsx:2671` 也叫“任务自动记录”。
- 本轮没有充分证明任务反复完成、撤销、跳过时自动日志是否会去重、删除，以及所有来源是否仅代表完成。
- **待确认：写入链路能否保证一任务一次有效完成事件。** 若不能，应按任务 ID 和有效完成事件统计，或诚实叫“任务自动日志条数”。时间窗口问题已由 B05 单独确认。

### D03 BCG 四象限与字段名不一致，但仅凭字段名不能断言内容错

- `pages/Strategy.tsx:144`—`:147` 把 `marketGrowthHigh / marketGrowthLow / marketShareHigh / marketShareLow` 分别标为明星 / 金牛 / 问号 / 瘦狗，并在 `:362` 渲染“BCG 业务矩阵分布”。
- 单看字段名，它们只描述增长或份额一个维度；但 AI 返回内容可能已经按四类组织。
- **待确认：生成端四字段的正式口径。** 四象限应该同时由增长和相对份额判定；未确认前不能把字段改名当作已完成修正。

### D04 客户“正常”与低风险是否同义

- 客户列表 `pages/Customers.tsx:1086`—`:1088`、`:1128`—`:1130` 将非 high 的 `customer.riskStatus` 显示“正常”；公共 `RiskBadge` 在 `src/ui/statusBadge.tsx:60`—`:64` 则支持中风险。
- **待确认：客户 riskStatus 的生产取值是否允许 medium/mid，以及空值如何解释。** 如果只允许 high/low，是二态设计；如允许三级，就会吞掉中风险。不能因为公共徽章支持三级就断言客户目前也一定有三级。

### D05 旧卡定义不是当前页面问题

- `pages/Dashboard.tsx:848` 有“待确认到款数”，`:852` 明说用结算草稿占位；但它属于旧 financeCards / kpiCards。
- 当前渲染 `pages/Dashboard.tsx:1206`—`:1213` 给财务的是 `dashboardMetrics.finance`，没有使用该旧卡；销售、顾问的旧卡同理。
- **结论：不算在屏幕上发现的问题，保留为旧代码残留说明。** 当前可见的结算草稿冒充未开票项目已单列 C01。

## 六、逐文件扫查记录

下表是抽取后核对的字段、状态主题和定位锚点，覆盖全部 47 个 TSX；“无新增”仅表示未另找出能用代码确认的冲突，不代表运行验收通过。纯样式、导入和注释不当作用户字段。

### 页面及工作台（24 个）

| 文件与锚点 | 核对的文案 / 字段族 | 结论 |
| --- | --- | --- |
| `pages/AICenter.tsx:60` | 配置、模型状态、知识资料、隐私开关、成员角色 | C11、C12；占位开关已明示未接入，不另报 |
| `pages/Audit.tsx:1235` | 不符合项状态、严重度、整改日期、证据、验证、趋势 | A01—A03、B03、C17—C18 |
| `pages/AuthAuditLogs.tsx:176` | 时间、动作、操作人、对象、详情 | A10 |
| `pages/ChangePassword.tsx:129` | 当前密码、新密码、确认新密码 | 无新增 |
| `pages/Contracts.tsx:1385` | 编号、客户、总额、回款计划、风险、归档、立项 | A12、B01、B03—B04、C19 |
| `pages/Customers.tsx:1027` | 主体信息、联系人、证书、合同汇总、PDCA、不符合项 | A01—A03、A05、B03、C09—C10、D01、D04 |
| `pages/Dashboard.tsx:1193` | 角色指标、趋势、风险、提醒、联动入口 | A08、B04、C04—C05、D05 |
| `pages/Employees.tsx:387` | 姓名、账号、角色、岗位、状态、有效期、上级 | C13；需改密文案已准确 |
| `pages/Finance.tsx:470` | 应收日期、节点、金额、状态、结算类型、对象、金额 | A12、B02、B04、B06、C03—C04；应收状态不重复报 |
| `pages/IntelRadar.tsx:740` | 情报统计、类型、处理状态、紧急度、评分、发布时间 | A11、C06 |
| `pages/Knowledge.tsx:470` | 分类、AI 许可、可见范围、摘要、审计状态/严重度 | A01—A02、A04、C11 |
| `pages/Leads.tsx:551` | 企业、联系人、AI 评分、目标证书、跟进、状态、工商字段 | A05—A06；线索状态共用徽章 |
| `pages/Login.tsx:100` | 账号、密码、常用电脑 | 无新增 |
| `pages/MyTasks.tsx:241` | 逾期、今天、本周、进行中、范围、任务状态 | B05；基础状态复用公共控件 |
| `pages/Projects.tsx:2971` | 项目、负责人、进度、金额、结算、服务项、任务、日志 | A01—A03、A07、A12、B01—B03、B05、C02、C07—C08、C16 |
| `pages/Strategy.tsx:407` | 待启动、攻坚中、已达成、战役负责人、SWOT、BCG | D03；战略战役不等同交付项目，不硬并状态 |
| `pages/dashboard/BossDashboard.tsx:55` | 经营卡、团队产能、指标悬浮口径 | B04—B05、C05；项目延误率另有人修 |
| `pages/dashboard/ConsultantDashboard.tsx:40` | 交付状态、交付风险、日志工时、参与项目 | B05、D02；追到 dashboardMetrics 动态标题 |
| `pages/dashboard/FinanceDashboard.tsx:13` | 现金流、账目异常、结构分析 | C01、C04、C16；追到 dashboardMetrics |
| `pages/dashboard/ManagerDashboard.tsx:39` | 待指派、任务、负责人负荷、日志覆盖 | B05；没有把项目数和人数硬并 |
| `pages/dashboard/PersonaDashboard.tsx:94` | 动态卡标题、值、说明、列表条数 | 已追各角色传入的 metrics |
| `pages/dashboard/RiskPanel.tsx:54` | 风险类型数、明细条数、严重程度 | A08；类型数与明细数本身有区分 |
| `pages/dashboard/SalesDashboard.tsx:13` | 成交指标、机会、今日联系、优先处理 | C15；追到 dashboardMetrics |
| `pages/dashboard/SysAdminBoard.tsx:159` | 错误、会话、密码、运行时长、反馈、AI 用量、数据规模 | A09、C14 |

### 公共组件及 UI（23 个）

| 文件与锚点 | 核对的文案 / 字段族 | 结论 |
| --- | --- | --- |
| `components/AIChatWidget.tsx:1053` | 上下文量、模型、引用、知识命中、执行回执 | 上下文量与每日额度已分开；不重复报告项目完成相关既有术语修复 |
| `components/AiProposalQueue.tsx:183` | 待我确认、批准、驳回原因、执行失败 | 与财务待确认是不同对象，区域清楚，不硬并 |
| `components/CrashBoundary.tsx:82` | 错误位置、刷新、返回工作台 | 无新增字段冲突 |
| `components/FeedbackModal.tsx:30` | 反馈类别、影响程度、期望、实际结果 | A09 |
| `components/HelpHub.tsx:537` | 工作台导览、模块讲解、字段解释、样例说明 | 未把帮助提示当业务统计 |
| `components/IngestionUploader.tsx:143` | 识别中、失败、重试、选择文件 | 无新增 |
| `components/Layout.tsx:186` | 页面名、角色视角、提醒严重度、条数/件数、账号菜单 | A08；提醒聚合条数与件数已注明换算 |
| `components/LoginSessions.tsx:160` | 登录设备、活跃时间、常用设备、下线 | 无新增；同设备多会话已注明 |
| `components/MonthlyReview.tsx:148` | 近三月签约、零签约月、逾期款项/任务、集中度 | 明确时间范围和单位；未把 AI 建议优先级与提醒严重度硬并 |
| `components/MyAiUsage.tsx:58` | 今日 AI 用量、额度、百分比 | 无新增；与聊天上下文不是同一概念 |
| `components/MyWorkWidget.tsx:93` | 我的活、逾期、这一周、任务截止日 | B05 |
| `components/OnboardingTour.tsx:46` | 六角色名、引导步骤、预览角色 | 正式角色与岗位标签有区分 |
| `components/ProjectCompleteChecklist.tsx:43` | 未完成任务数、完成、跳过、结项前清单 | 完成与跳过分别选择；不重复报已处理的项目结项术语 |
| `components/ProtectedRoute.tsx:45` | 页面未开放、权限不足提示 | 无业务字段新增 |
| `components/SampleRow.tsx:69` | 样例、非真实数据 | 明确标识，不计真实统计 |
| `components/ServicePicker.tsx:157` | 标准目录项、非标准、合同原文、自定义项 | 已区分标准服务与原文输入 |
| `components/Sidebar.tsx:140` | 模块入口名、回款概览、顾问结算 | 作为导航核对；模块名差异未一概计作字段问题 |
| `components/StateSyncNotice.tsx:14` | 未保存、导出未保存内容、重试保存 | 无新增 |
| `components/TaskSkipButton.tsx:27` | 已跳过、原因、恢复待办 | 为 C07—C08 提供任务状态基准 |
| `components/TaskStatusControl.tsx:49` | 完成、待开始、进行中、前置阻塞 | 为 C08 提供状态动作基准 |
| `components/VersionWatcher.tsx:131` | 更新、断线、刷新、稍后 | 无新增业务字段 |
| `src/ui/index.tsx:71` | 搜索、选中、关闭、动态卡标签 | 通用表头由调用方传入，已回溯调用方 |
| `src/ui/statusBadge.tsx:25` | 线索/项目/合同状态映射、风险级别 | 作为既有口径对照；客户风险是否三级放 D04 |

## 七、建议处理顺序

先处理会让人误判钱、风险或完成情况的 C01—C09、C14—C16、C19，以及 B04—B06 的指标口径。随后把不符合项状态、严重程度、知识分类和情报紧急度收为共用显示映射，覆盖桌面、手机、弹窗与导出。最后处理表单字段名、反馈和提示语。

这里只给修正建议；没有修改任何业务代码，也没有把静态判断写成已经修复或运行验证通过。
