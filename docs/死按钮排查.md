本轮确认死按钮 **0 处**，拿不准的 **0 处**；已检查 components/ 和 src/ui/ 下全部 23 个 TSX 文件，共 92 处原生按钮定义，另核对 StatCard 的 2 处动态按钮渲染分支。

## 范围和判据

- 基准提交：5089e00；开始执行 git log --oneline -5 已确认，结束复核提交未变。日期：2026-09-16。
- 只读业务代码，没有打开浏览器、启动服务或实际点击。这里的结论只针对“按钮没有接动作”，不代表接口、权限、保存结果等运行行为全部通过。
- 按源码定义计数：循环里的同一按钮算一处；不按页面实例数重复计算。
- 用 rg --files 枚举两个目录，并用 TypeScript 语法解析读取 JSX 开始标签、自闭合标签和属性，避免多行 onClick 或箭头函数造成正则漏判。全部原生按钮都带 onClick；没有缺少直接事件、需要依靠 submit 或祖先冒泡才能成立的候选。
- 同时检索并核对动态标签、属性展开、input 按钮类型、role="button"、createElement 和按钮样式入口；未发现额外的无动作按钮。buttonClass 只是样式函数，不会生成按钮。
- 判死需同时满足：自身没有点击或按下事件；没有有效的表单提交动作（包括核对外层 form）；祖先也没有能产生实际动作的点击事件。仅 stopPropagation 不算业务动作。文案明确标注开发中、禁用状态和 SampleRow / SampleList 演示拦截按要求排除。

## 已确认的死按钮

无。本次不把 pages/ 中上一轮的“发起结算”“导出对账单”算入新增结果；它们不在本轮扫描目录内，本轮也没有修复它们。

## 逐文件核对表

下列行号均为 button 开始标签所在行。表内 92 处均有直接 onClick（缺失数 0）；零按钮的文件也列出，便于核对范围。

| 文件 | 原生按钮数 | 开始行号 |
| --- | ---: | --- |
| components/AIChatWidget.tsx | 9 | 987、988、1126、1131、1169、1194、1197、1204、1218 |
| components/AiProposalQueue.tsx | 6 | 187、213、230、236、244、250 |
| components/CrashBoundary.tsx | 1 | 108 |
| components/FeedbackModal.tsx | 5 | 103、115、128、177、201 |
| components/HelpHub.tsx | 13 | 398、449、469、510、518、530、544、558、591、593、594、598、599 |
| components/IngestionUploader.tsx | 0 | 无原生按钮 |
| components/Layout.tsx | 20 | 548、572、622、670、709、753、778、795、810、819、888、895、909、972、991、1031、1051、1069、1103、1143 |
| components/LoginSessions.tsx | 2 | 162、226 |
| components/MonthlyReview.tsx | 3 | 126、264、319 |
| components/MyAiUsage.tsx | 0 | 无原生按钮 |
| components/MyWorkWidget.tsx | 2 | 100、165 |
| components/OnboardingTour.tsx | 6 | 346、447、460、466、471、479 |
| components/ProjectCompleteChecklist.tsx | 4 | 56、62、86、90 |
| components/ProtectedRoute.tsx | 0 | 无原生按钮 |
| components/SampleRow.tsx | 0 | 无原生按钮 |
| components/ServicePicker.tsx | 5 | 161、182、226、251、281 |
| components/Sidebar.tsx | 3 | 117、152、189 |
| components/StateSyncNotice.tsx | 2 | 18、19 |
| components/TaskSkipButton.tsx | 5 | 33、46、60、75、81 |
| components/TaskStatusControl.tsx | 2 | 67、81 |
| components/VersionWatcher.tsx | 2 | 139、149 |
| src/ui/index.tsx | 2 | 82、316 |
| src/ui/statusBadge.tsx | 0 | 无原生按钮 |

## 容易误判的共享入口与引用方

| 文件:行号 / 文案 | 所在组件 | 引用方（rg 检索后核对） | 为什么不判死 |
| --- | --- | --- | --- |
| src/ui/index.tsx:224、240 / 由 label 传入 | StatCard | pages/Projects.tsx:2742、2751、2760、2769；pages/MyTasks.tsx:241–244 | 第 216 行根据 onClick 选择 button 或 div；渲染 button 时同时展开 onClick。项目页四处传入筛选动作；我的任务页未传回调，实际是展示块。 |
| components/Sidebar.tsx:117 / X 图标（无 title、aria-label） | Sidebar | components/Layout.tsx:854，随主布局使用 | 第 116 行要求 onClose 存在才渲染；Layout 传入收起侧栏动作。 |
| components/MonthlyReview.tsx:319 / 收进战役（已收进战役） | MonthlyReview | pages/Strategy.tsx:309 | 第 318 行要求 onAdopt 存在才渲染；策略页传入 adoptMonthlyAction。 |
| components/OnboardingTour.tsx:346、447、460、466、471、479 / 引导关闭、前后步、开始上手等 | OnboardingTour | components/Layout.tsx:1154，随主布局使用 | finish 自己关闭引导，learnPage 会导航，go 会换步骤；可选回调未提供不会使这些核心动作消失；返回帮助按钮还受 onHelp 条件保护。Layout 实际传齐回调。 |
| src/ui/index.tsx:82 / 清除搜索 | SearchInput | Customers、Contracts、Leads、Audit、Projects、MyTasks 等页面的 SearchInput 调用 | onClick 调用 onChange('')，已核对页面传入搜索状态更新函数。 |
| src/ui/index.tsx:316 / aria-label“关闭” | Modal | 仓库 TSX 检索未见实际调用 | 直接绑定必传 onClose；当前没有可见的调用实例，不能臆造一个没传回调的页面。 |
| components/IngestionUploader.tsx:124、171 / 选择文件、上传区 | IngestionUploader | Customers、Contracts、Leads、Knowledge 页面 | 两种布局都有铺满入口的透明 input type=file，浏览器原生选择文件，onChange 接 handleChange；文案外壳无需另挂 onClick。 |
| src/ui/index.tsx:135 / 筛选外壳 | FilterSelect | Customers、MyTasks、Projects 页面 | 透明 select 覆盖外壳并绑定 onChange，是原生下拉选择，不是死按钮。 |
| components/SampleRow.tsx:56、102、141 / 样例数据 | SampleRow、SampleTr、SampleList | Customers、Contracts、Leads、Projects、Finance 等页面 | 明确标识样例，并在捕获阶段 preventDefault / stopPropagation；按要求排除，不把演示点击拦截算故障。 |

引用链补充：Layout 由 App.tsx:4 引入，承载各业务页面的共用侧栏、帮助和引导。引用检索使用 rg -n 配合组件名及 components/、src/ui 导入路径，范围包含 pages/ 子目录与 App.tsx；读取引用方只是为了核对共享组件，不将页面自有按钮扩入本轮统计。

## 拿不准的

无。本轮未留下需要依靠浏览器才能判断“有没有接动作”的候选。已经绑定动作的按钮是否执行成功、是否受运行时条件影响，属于另一类验证，本报告不据此宣称通过。
