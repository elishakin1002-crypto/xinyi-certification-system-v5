# 新手引导与项目列表修复验收

本次发布基于 2c4c60a，保留三层入口：认识岗位工作台、了解当前模块、解释具体控件。

- 总经理、销售、咨询顾问、财务、总助、系统管理员分别验证岗位目标、六步介绍和第一项上手任务，覆盖桌面和手机。
- 客户、合同、项目、线索、不符合项、回款、结算、员工、审计日志等样例使用真实列表的同一个渲染回调；知识、情报、战略任务也使用真实卡片。我的任务复用真实任务行。
- 样例仅供观察，不入业务数组、不持久化、不计入统计；引导中或空列表时出现。有真实数据时退出引导即隐藏。
- 项目统计卡片增加选中边框、已选中标签和辅助访问状态。卡住项目按未完成超期任务筛选；红色卡片展示具体任务清单和涉及项目数。
- 新建项目等待接口保存、需要时读回校验，成功才关闭表单和提示；失败保留输入。合同、战略、AI 建项目入口同步检查保存结果。
- 修复手机帮助把隐藏 AI 面板误识别成当前弹窗的问题；宽列表讲解优先放在目标下方，减少遮挡。
- 默认回滚选择最新的部署前快照，保持数据库和生产配置。

## 验证方式

岗位与页面检查使用隔离浏览器上下文及合成记录，未向生产创建演示数据。六类角色 × 两种视口共 12 组岗位引导检查；20 条角色/模块组合 × 两种视口共 40 组页面检查（战略默认月度复盘不展示假统计）。另验证四张项目卡片结果数与选中状态、保存失败及重试成功两条路径。

运行全量测试、类型与生产构建、前端密钥扫描、权限矩阵检查和迁移文件检查。完整日志与浏览器截图保存在本次任务的 .artifacts 和本机 /tmp/xinyi-final-*.txt。

## 发布边界

本次在 codex/onboarding-review-20260909 独立分支准备发布，不夹带共享目录中其他任务的账号、后端启动和自动跟进调整。没有新增数据库迁移。

部署前由 deploy/deploy.sh 保存当前线上版本；如需回退，使用 deploy/rollback.sh 并指定本次部署前快照名称。回退只恢复代码，不删除同事新录入的数据。

## 发布结果

已部署版本：34464d2。线上服务、PostgreSQL 连接、首页和静态资源检查通过；未登录业务接口返回 401。线上登录页在 1440px 与 390px 视口均通过加载、资源和溢出检查。

全量测试：465 项，462 通过、3 跳过、0 失败；针对样例、帮助及回滚的 18 项检查全部通过。

本次部署前快照：`/opt/xinyi-releases/20260909-142854-72b54d7.tar.gz`（已核实存在）。精确回滚命令：

```sh
bash deploy/rollback.sh 20260909-142854-72b54d7.tar.gz
```

六角色业务引导检查在隔离测试页面完成；上线后检查了服务、鉴权及登录页面，没有在生产添加测试业务记录。
