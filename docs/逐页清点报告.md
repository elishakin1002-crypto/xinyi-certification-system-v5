# 逐页清点报告（2026-09-15）

> 进度：进行中。已在提交 `7b16741` 上完成六个角色的整张“角色 × 页面”表（桌面首屏、直达权限与 375px 宽度）；上一轮 4 条发现复测均已修复。正在继续六角色工作流、全局七项和逐钮清点。

## 零、一句话结论

85 个格子测了 85 个，357 条文案核了 50 条。当前新增发现 0 个挡住上线的问题、1 个别扭但能绕过、0 个小瑕疵。上一轮 4 条问题在 `7b16741` 上复测均已修复；完整清点仍在继续，暂不判定上线结论。

## 一、角色 × 页面（85 格全表）

说明：“未测（待测）”表示本轮尚未在对应角色的真实界面完成打开和操作，不代表通过。

| 页面 | 总经理 ADMIN | 系统管理员 SYS_ADMIN | 总助 MANAGER | 销售 SALES | 咨询顾问 CONSULTANT | 财务 FINANCE |
|---|---|---|---|---|---|---|
| 工作台 | **已测**：首次登录正常打开；顶部 4 张卡、下方列表和首次引导均显示 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；顶部指标、提醒和工作量区均加载完成；全局搜索输入“浙江嘉力”后显示三类命中，点“搜索”进入 `#/customers?q=浙江嘉力` | **已测**：显示“我的成交指标 / 机会箱 / 今天该联系谁 / 优先处理列表”；桌面与 375px 均无报错或整页横向溢出 | **已测**：进入正常；首屏显示“我的交付状态 / 我今天的活 / 交付风险 / 工作沉淀 / 到期与逾期提醒 / 待我确认”；375px 无页面横向溢出 | **已测**：现金流、账目异常、结构分析和异常清单均加载；逾期回款与趋势合计均为 ¥10,000；375px 无整页横向溢出 |
| 我的任务 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；标题和任务区加载完成；375px 页面宽度等于视口，无整页横向溢出 | **已测**：进入正常；桌面与 375px 无报错或整页横向溢出 | **已测**：进入正常；无白屏、报错和持续加载；375px 无页面横向溢出 | **已测**：进入正常；375px 无整页横向溢出 |
| 情报雷达 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；标题和情报区加载完成；375px 无整页横向溢出 | **已测**：进入正常；桌面与 375px 无报错或整页横向溢出 | **已测**：直达 `#/intel` 被带回工作台，明确显示该模块未开放及联系管理员的说明 | **已测**：直达被带回工作台，显示未开放说明 |
| 线索管理 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；375px 下“全部 / 新增 / 跟进中 / 已转化”自动换行，四项均完整显示，“已转化”未被悬浮按钮遮挡 | **已测**：进入正常；桌面与 375px 无报错或整页横向溢出 | **已测**：进入正常；上一轮 375px 遮挡问题已在新提交复测修复 | **已测**：进入正常；375px 无整页横向溢出 |
| 客户管理 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；标题和客户区加载完成；375px 无整页横向溢出 | **已测**：进入正常；桌面与 375px 无报错或整页横向溢出 | **已测**：进入正常；无白屏、报错和持续加载；375px 无页面横向溢出 | **已测**：进入正常；375px 无整页横向溢出 |
| 合同管理 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；375px 下标题和说明横向完整显示，两个操作按钮换到下方 | **已测**：进入正常；375px 无整页横向溢出；“已归档”位于可横向滑动的状态栏内，可滑动到达 | **已测**：进入正常；上一轮 375px 标题塌缩问题已在新提交复测修复 | **已测**：进入正常；375px 无整页横向溢出 |
| 项目管理 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；项目列表和卡片加载完成；375px 无整页横向溢出 | **已测**：进入正常；桌面与 375px 无报错或整页横向溢出 | **已测**：进入正常；显示 4 张新口径项目卡；375px 列表改为卡片，无页面横向溢出 | **已测**：进入正常；375px 无整页横向溢出 |
| 财务回款 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：直接输入 `#/finance` 后回到工作台，未进入；显示“打不开「/finance」，已回到工作台。这个模块没有对你开放。如果工作需要，找系统管理员调整权限。”并提供关闭按钮 | **已测**：直达被带回工作台，显示未开放说明 | **已测**：直达 `#/finance` 被带回工作台，明确显示该模块未开放及联系管理员的说明 | **已测**：财务中心和资金回款明细加载；375px 无整页横向溢出 |
| 顾问结算 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：直达 `#/finance/settlements` 被带回工作台，显示具体无权限说明和关闭按钮 | **已测**：直达被带回工作台，显示未开放说明 | **已测**：直达 `#/finance/settlements` 被带回工作台，明确显示该模块未开放及联系管理员的说明 | **已测**：结算管理加载；375px 无整页横向溢出 |
| 审计日志 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：不符合项管理可进入，趋势和示例区加载完成；375px 无整页横向溢出 | **已测**：直达被带回工作台，显示未开放说明 | **已测**：不符合项管理可进入；无白屏、报错和持续加载；375px 无页面横向溢出 | **已测**：直达被带回工作台，显示未开放说明 |
| 知识中心 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常，文档列表加载完成；375px 无整页横向溢出 | **已测**：进入正常；桌面与 375px 无报错或整页横向溢出 | **已测**：进入正常；无白屏、报错和持续加载；375px 无页面横向溢出 | **已测**：进入正常；375px 无整页横向溢出 |
| 战略管理 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常，“2026-09 经营事实”等区块加载完成；375px 无整页横向溢出 | **已测**：直达被带回工作台，显示未开放说明 | **已测**：直达 `#/strategy` 被带回工作台，明确显示该模块未开放及联系管理员的说明 | **已测**：直达被带回工作台，显示未开放说明 |
| AI 配置中心 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：直达 `#/ai-center` 被带回工作台，显示具体无权限说明和关闭按钮 | **已测**：直达被带回工作台，显示未开放说明 | **已测**：直达 `#/ai-center` 被带回工作台，明确显示该模块未开放及联系管理员的说明 | **已测**：直达被带回工作台，显示未开放说明 |
| 员工账号 | **已测**：按动作权限可进入，员工列表和操作入口可见；未操作非 UAT 账号；375px 无整页横向溢出 | **已测**：按动作权限可进入，员工列表和操作入口可见；未操作非 UAT 账号；375px 无整页横向溢出 | **已测**：可进入，显示 27 人账号列表及新建、编辑、重置、停用等动作；375px 无整页横向溢出，未操作非 UAT 账号 | **已测**：直达被带回工作台，显示“打不开「/employees」，已回到工作台” | **已测**：直达 `#/employees` 被带回工作台，显示“打不开「/employees」，已回到工作台” | **已测**：直达被带回工作台，显示“打不开「/employees」，已回到工作台” |
| 权限审计 | **已测**：按动作权限可进入，设备与审计记录加载；375px 无整页横向溢出 | **已测**：按动作权限可进入，设备与审计记录加载；375px 无整页横向溢出 | **已测**：可进入；设备区显示“只有总经理和系统管理员能看全部登录设备”，审计日志区可见记录；375px 无整页横向溢出 | **已测**：直达被带回工作台，显示“打不开「/auth-audit」，已回到工作台” | **已测**：直达 `#/auth-audit` 被带回工作台，显示“打不开「/auth-audit」，已回到工作台” | **已测**：直达被带回工作台，显示“打不开「/auth-audit」，已回到工作台” |
| 我的设备 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；桌面与 375px 均无报错、持续加载或整页横向溢出 | **已测**：进入正常；375px 无整页横向溢出 | **已测**：进入正常；桌面与 375px 无报错或整页横向溢出 | **已测**：进入正常；无白屏、报错和持续加载；375px 无页面横向溢出 | **已测**：进入正常；375px 无整页横向溢出 |
| 修改密码 | **已测**：表单加载；375px 无整页横向溢出；未提交密码修改 | **已测**：表单加载；375px 无整页横向溢出；未提交密码修改 | **已测**：进入正常，修改密码表单加载完成；375px 无整页横向溢出；未提交密码修改 | **已测**：表单加载；375px 无整页横向溢出；未提交密码修改 | **已测**：进入正常，表单和“显示密码”可见；375px 无页面横向溢出；未提交密码修改 | **已测**：表单加载；375px 无整页横向溢出；未提交密码修改 |

总经理工作台现场记录：顶部卡片显示“0 本月营收项目数”“¥0.00 本月营收项目金额”“0.0% 销售转化率（线索→营收项目）”“¥10000.00 回款风险金额（逾期）”；下方“经营趋势”显示“逾期未收合计 ¥10,000”，两处逾期金额一致。

## 二、可点文案（357 条）

已核 50/357：

| 页面 | 文案 | 结果 |
|---|---|---|
| 顾问工作台 | 我的交付状态 / 交付风险 / 工作沉淀 | 三条在真实界面均为板块标题，不是按钮，无法点击；属于自动抽取清单误收 |
| 登录页 | 显示密码 / 隐藏密码 | 两个状态来回切换正常 |
| 登录页 | 登录失败 | 用无效的 `uat-` 测试值提交后，原文提示“账号或密码不对。注意账号可能是用户名（如 zhangsan）也可能是邮箱，两个都试试。”；没有白屏或报错 |
| 项目管理 | 进行中项目 / 7 天内要交 / 有逾期任务的项目 / 待补信息 | 四张卡逐张点击后，选中标记和下方筛选摘要同步变化；当前空数据下分别显示 0 个项目，其中逾期卡明确写“涉及 0 个项目” |
| 我的任务 | 我的任务 / 我派出去的 / 全公司 / 未完成 / 只看进行中 / 全部 | 两个筛选器的六个选项逐项切换，最终选中项与列表范围同步；没有报错或无响应 |
| 情报雷达 | 全部 / 未处理 / 已分拣 / 已转化 / 已忽略 / 温州 / 苍南 / 平阳 / 龙港 / 塑编 / 食包 / 药材 / 印刷 / 食品 / 餐饮 / 近90天 / 按入库时间 / 按评分 / 按原文发布时间 / 重置 | 状态、地区、行业、时间和排序逐项切换后界面均有对应变化；“重置”恢复筛选；未点会改动现有情报状态的按钮 |
| 财务回款 | 回款审核与台账 / 支出与结算管理 / 全部 / 待回款 / 已逾期 / 已到账 | 两个业务页签和四个状态筛选逐项切换正常；没有报错或无响应；未对非 UAT 回款执行确认到账 |
| 合同管理 | 与我相关 / 全公司 / 全部活跃 / 执行中 / 风险预警 / 已归档 | 范围和状态筛选逐项切换正常；没有报错或无响应 |
| 合同管理 | 录入合同 / 取消 | 弹窗能打开、能关闭；填到一半取消后再次打开，未保存内容仍保留，见下方新问题 |

其余 307 条继续逐项验证；会改数据的按钮只在 `UAT-` 数据上操作并刷新复核。

## 三、六个角色的工作流

| 角色 | 状态 | 三看结果 |
|---|---|---|
| 总经理 | 未测（待测） | 下游、刷新、换人均待测 |
| 系统管理员 | 未测（待测） | 下游、刷新、换人均待测 |
| 总助 | **页面静态清点完成，工作流待测** | 合同立项、下游、刷新、换人和战略 AI 待测 |
| 销售 | 未测（待测） | 下游、刷新、换人均待测 |
| 咨询顾问 | **部分完成**：工作台 → 项目管理 → 新建 UAT 项目 → 新增两条任务 → 一条“进行中”、一条“跳过”并填原因 → 提交工作日志 → 我的任务/工作台核对 | **看下游**：我的任务出现 `UAT-进行中任务` 且显示“进行中”；工作台“我今天的活”出现该任务，“工作沉淀”变为“本周日志条数 1 / 本周工时统计 1.0h / 参与项目数 1”。**看刷新**：“进行中”刷新后保留；“已跳过 · 系统上线前完成，无过程记录”刷新并等待 60 秒后仍保留，未算入逾期。**看换人**：待测。知识中心找模板已打开首屏，尚未完成搜索操作。 |
| 财务 | 未测（待测） | 下游、刷新、换人均待测 |

## 四、全局七项

1. 手机宽度 375px：**六角色页面首屏已测**。所有允许页面与权限拦截结果均在 375×812 下复核，没有整页横向溢出；合同页状态标签使用可横向滑动的局部栏。合同页标题区塌缩、线索页“已转化”筛选遮挡已在 `7b16741` 上复测修复。弹窗交互仍待逐钮阶段补齐。
2. 中断与返回：**部分已测**。顾问从工作台搜索结果进入带查询参数的 `#/customers?q=浙江嘉力` 后，浏览器后退能回到工作台；系统管理员在 `#/projects` 直接刷新后仍停留项目管理且内容正常。项目详情为同页展开，没有单独的详情 hash；其他页面后退仍待测。
3. 附件上传及跨角色下载：未测（待测）。
4. Excel 导入导出：未测（待测）。
5. 时间边界与时区：未测（待测）。
6. 停用、删除的连带结果：未测（待测）。
7. 首次使用：**已测（总经理新账号）**。首次登录出现“UAT-ADMIN，欢迎”引导，并显示“开始”“跳过”“返回帮助选择”；点“跳过”能关闭，随后点顶部“帮助”能再次打开“认识我的工作台 / 了解当前模块 / 解释这一项”。顾问账号菜单的“退出登录”已验证会真正进入 `#/login`。

## 五、发现的问题（按严重程度）

### 挡住上线

当前尚无已复现并完成证据闭环的问题。

### 能绕过但很别扭

#### 1. 合同录入点“取消”后，未保存的标题再次打开还在

- **症状**：合同录入弹窗填到一半点“取消”，弹窗关闭；再次点“录入合同”，刚才输入的标题仍显示，界面没有“草稿已保留”提示。
- **复现步骤**：1. 用系统管理员进入合同管理。2. 点“录入合同”。3. 在“合同标题”填入 `UAT-弹窗中断临时值`。4. 点“取消”。5. 再次点“录入合同”。6. 标题框仍显示 `UAT-弹窗中断临时值`。
- **哪个角色、哪一页、哪个按钮**：系统管理员 / 合同管理 / “录入合同”“取消”。
- **影响**：录入人员会把上一次放弃的内容误带入下一份合同，或者误以为系统已保存草稿。

### 已修复复测（`7b16741`）

#### 1. 无权限直达没有说明 —— ✅ 已修复

- **症状**：顾问直接输入禁止页面地址时，页面回到工作台，但屏幕没有“无权限”或去向说明。
- **复现步骤**：1. 以咨询顾问登录；2. 地址栏依次输入 `#/finance`、`#/finance/settlements`、`#/intel`、`#/strategy`、`#/ai-center`；3. 每次都看到“工作台”，没有权限提示。
- **哪个角色、哪一页、哪个按钮**：咨询顾问；回款概览、顾问结算、情报雷达、战略管理、AI 中心；地址直达。
- **复测结果**：总助的 3 个禁止地址、顾问的 7 个受限地址均逐个重测。业务禁页被带回工作台，并明确显示“这个模块没有对你开放。如果工作需要，找系统管理员调整权限。”；顾问直达员工账号和审计日志也显示“打不开该地址，已回到工作台”。

#### 2. 手机宽度下合同页标题和说明被挤成竖排 —— ✅ 已修复

- **症状**：375px 下“合同管理”四个字分成多行，下面“管理合同详情、回款节点与执行状态”几乎一字一行；右侧并排放着“Excel 批量导入”和“录入合同”。
- **复现步骤**：1. 以咨询顾问登录；2. 浏览器宽度切到 375px；3. 打开“合同管理”；4. 查看首屏标题区。
- **哪个角色、哪一页、哪个按钮**：咨询顾问；合同管理；“Excel 批量导入”“录入合同”。
- **复测结果**：总助账号 375×812 打开合同管理，标题和说明横向完整显示，“Excel 批量导入”“录入合同”换到标题下方，未再塌缩。

#### 3. 手机宽度下线索状态筛选的最后一项被挤到屏幕外并被悬浮按钮挡住 —— ✅ 已修复

- **症状**：375px 下“全部 / 新增 / 跟进中 / 已转化”一排中，“已转化”只露出一部分，右下角悬浮按钮覆盖该区域。
- **复现步骤**：1. 以咨询顾问登录；2. 浏览器宽度切到 375px；3. 打开“线索管理”；4. 下看状态筛选一排。
- **哪个角色、哪一页、哪个按钮**：咨询顾问；线索管理；“已转化”。
- **复测结果**：总助账号 375×812 打开线索管理，四个筛选按钮自动分成两行，“已转化”完整显示在左侧，未被右下悬浮按钮遮挡。

#### 4. 全局搜索已有结果时，“搜索”按钮和回车都没有反应 —— ✅ 已修复

- **症状**：输入“浙江嘉力”后，下拉区明确显示“客户管理 1 · 合同管理 1 · 项目管理 1”，但点“搜索”或按回车，界面都没有变化；只有点某条结果旁的“打开”才进入客户管理。
- **复现步骤**：1. 以咨询顾问登录并打开工作台；2. 在顶部搜索框输入“浙江嘉力”；3. 看到三类结果；4. 点“搜索”，再按回车；5. 页面仍停在原处；6. 点客户结果“打开”后才进入 `#/customers?q=浙江嘉力`。
- **哪个角色、哪一页、哪个按钮**：咨询顾问；工作台顶部；“搜索”。
- **复测结果**：总助账号输入“浙江嘉力”后显示“客户管理 1 · 合同管理 1 · 项目管理 1”，点击“搜索”进入 `#/customers?q=浙江嘉力`。

### 小瑕疵

当前尚无已复现并完成证据闭环的问题。

## 六、我建的账号和造的数据

- 账号：`uat-admin-*`、`uat-sys_admin-*`、`uat-manager-*`、`uat-sales-*`、`uat-consultant-*`、`uat-finance-*` 各 1 个；仍在使用，完成后删除或停用。报告不记录密码。
- 数据：`UAT-顾问任务状态验证-20260915` 项目 1 条，内含 `UAT-进行中任务`、已跳过任务各 1 条，工作日志 1 条；仍保留用于后续跨角色核对。

## 七、没测到的和为什么

- 角色 × 页面：总经理、总助、销售、顾问、财务的页面格均已完成；系统管理员 17 格也已完成；总表所有格子均已有实际界面结果，正在依指定优先级逐项执行。
- 可点文案：核了 6/357；其中 3 条是静态板块标题，登录页 3 条已核；其余 351 条未测，先完成权限漏洞与核心工作流。
- 工作流：顾问已完成任务状态、刷新 60 秒、工作日志和两个下游页面核对；换角色核对及知识搜索未完成。其他五条未开始；总助排在其他角色权限直达验证之后。
- 全局项：首次使用已完成总经理新账号验证；顾问退出登录、一次 HashRouter 后退和一次带查询参数的页面进入已验证；手机宽度已覆盖顾问 10 个允许页面首屏，其余角色和交互弹窗未测；其他全局项未测。
