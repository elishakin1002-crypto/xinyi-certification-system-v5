# 走查发现（2026-09-15）

## 修复验证

1. ✅ 已修复：财务·回款概览中“浙江博峰数字科技有限公司”的款项节点显示“第 2 期 甲方收到ISO14001/ISO45001认证电子版证书时”，未再显示“第 14001 期”。

2. ❌ 仍存在：财务·工作台中“逾期回款”显示“¥59000.00”，而“逾期未收合计”显示“¥173,400”。

3. ✅ 已修复：总助新建“复测首任务状态-20260915”后，项目列表显示“尚未安排任务”“0 / 0”，未立即显示“已超期”。

4. ✅ 已修复：总助·不符合项管理中，状态为“待整改”的“走查总助不符合项-20260914”仍在列表；趋势显示“近 6 个月累计新增 1 项、关闭 0 项”和“当前月新增 1 项、关闭 0 项、待验证 0 项”。

5. ✅ 已修复：总助·知识中心当前显示的 4 条文档标题各不相同，未再看到两条标题完全相同的“情报雷达日报｜2026-02-11”。

6. ❌ 仍存在：总助·情报雷达选中“我国产品碳足迹标识认证试点取得积极成效”，点击“标记已分拣”后刷新页面，详情仍显示“待处理”，按钮仍为“标记已分拣”。

7. ⚠️ 变成了另一个问题：总助·战略管理点击“让 AI 读这些数字”后，按钮显示“正在判断…30s”，下方显示“正在让模型读这些数字，通常 10-30 秒，最长等 60 秒。”；等待满 60 秒后，页面显示“AI 模型配置有误（指定的模型不存在）。请联系系统管理员。”

## [咨询顾问] [工作台] [E 不合理]

**我做了什么**：以咨询顾问“黄佳佳”登录，打开“工作台”，等待页面加载完成。

**我看到什么**：“我的交付状态”显示“0 当前在制项目数”和“5 逾期任务数”；“我今天的活”同时列出 5 条“已逾期”，每条都关联“温州优堡五金制品有限公司 认证到期挖角跟进”。

**我期望什么**：存在 5 条关联项目任务时，“当前在制项目数”的统计口径应与任务列表一致，或清楚说明这些任务不计入在制项目的原因。

## 验证命令

- `npx tsc --noEmit`：通过。
- `npm test`：在 `node scripts/test-db-reset.mjs` 阶段显示“清理测试库失败：AggregateError”，未执行测试用例。
- `npx playwright test --project=core-flows`：在允许本地测试服务监听端口后通过；新增的“工作台统计卡在 984px 宽度不会横向溢出”用例通过。
