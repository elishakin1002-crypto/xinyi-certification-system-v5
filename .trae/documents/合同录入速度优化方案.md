# 合同录入速度优化方案（使用 Kimi K2.5）

## 1. 变更说明
根据您的要求，我将把合同识别使用的模型切换为 **Kimi K2.5**。该模型在国内网络环境下更稳定，能有效降低连通性问题并保持较快响应。

## 2. 具体步骤
1.  **修改模型参数**：
    - 文件：`services/ingestion/contract.ts`
    - 操作：将 `aiService.generateJSON` 的第一个参数统一为 `'kimi-k2-0905-preview'`。

2.  **更新用户提示**：
    - 文件：`components/IngestionUploader.tsx`
    - 操作：将提示文案从 “可能需要 5-10 秒” 优化为 “正在极速分析中...”，以匹配更快的处理速度。

## 3. 验证
修改完成后，您可再次尝试录入合同，体验速度的提升。
