严格按照您的要求执行，不再新增其他方案。

### 执行步骤
1.  **修改 `importService.ts` (核心映射)**：
    *   明确将 Excel 解析出的 `capital`, `address`, `scope`, `legalRep` 等变量，直接赋值给 Lead 对象的 `registeredCapital`, `registeredAddress`, `businessScope`, `legalRepresentative` 等标准字段。
    *   确保这些字段是直接在 Lead 根属性上，而非嵌套在备注或 raw 数据中。

2.  **修改 `importService.ts` (去重逻辑)**：
    *   **第一重筛查**：检查 `unifiedSocialCreditCode` 是否存在且已在库中。
    *   **第二重筛查**（降级策略）：若无信用代码，则检查 `companyName`（去空格后）是否已在库中。
    *   **结果处理**：命中任意一条，立即 `return`（跳过），不添加到 `newLeads` 数组。

3.  **验证与报告**：
    *   任务完成后，我将输出一份【字段对照表】，逐行展示“赋值代码”与“UI读取代码”的一一对应关系，确保完全一致。