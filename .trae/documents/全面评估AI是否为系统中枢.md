## 评估结论（基于现有代码与文档）
- AI 在“交互入口层”已经是全局中枢：AI 聊天挂件在全局布局中注入，任何页面都能触发 AI 并可反向写入客户/合同/提醒等主数据。
- AI 在“系统主链路/数据真相源”层面还不是中枢：业务主数据与流程仍由 AppContext + LocalStorage 驱动；AI 能力可失败降级（后端不启动也能跑），属于增强件而非不可替代的核心引擎。

## 我将补齐的检查项（不改代码，仅输出审计报告）
- 画出 3 条关键闭环的“函数级调用链”：
  - 全局 ChatWidget 执行动作闭环（含 <execute_action> 协议与写回点）
  - Project AI 诊断中心闭环（aiInsight + aiDecisionLogs + 自动提醒）
  - 合同/财务/OCR 的结构化抽取闭环（从文件到字段回填/导入）
- 做一次“中枢性判定”打分：入口统一性、决策权、状态持久化、失败是否可用、是否强制治理（权限/审计/幂等）。

## 风险与口径对齐（提出两种改造方案）
- 识别并列出“文档口径 vs 实现”的差异点（重点：项目域铁律与 ChatWidget 可直接创建提醒/客户/合同的跨域写入张力）。
- 方案 A（让 AI 真正成为大脑中枢）：
  - 把所有 AI 执行动作统一收敛到“Project Execution Domain”，动作必须绑定 projectId；
  - 动作执行走“权限点 + 审计日志 + 幂等键”，默认需要用户确认；
  - ChatWidget 只产出建议与草稿，真正落库由 Project/Workflow 执行器完成。
- 方案 B（保持 AI 为强助手但不做中枢）：
  - 保留全局入口，但禁止自动写主数据；
  - 将 <execute_action> 改为“待确认任务列表”，用户点击后才写入；
  - 将项目诊断的自动提醒降级为“建议提醒”。

## 交付物与验收标准（若你确认需要我改造，再进入执行）
- 交付：一份系统审计报告（含证据链接）+ 一份“中枢化/非中枢化”改造设计（含接口契约、权限点、错误码与日志脱敏策略）。
- 验收：
  - AI 不可用时核心业务仍可运行；
  - AI 可用时所有写动作可追溯（谁触发、模型、输入摘要、输出、执行结果）；
  - 遵循“没有项目不得提醒/判断/扫描/执行”的铁律（若选方案 A）。

如果你确认这份计划，我再进入下一步：输出完整审计报告并按你选的方案实施改造。