# 恢复演示模式并修复 UI 遮挡方案

收到！既然咱们现在的目标是**先跑通演示版**，那我就立刻停止所有的后端改造计划，继续完善当前的前端高保真 Demo。

关于您提到的 **“信义全能大脑入口不见了”** 的问题，经过排查，这是一个 **UI 遮挡 bug**：
*   **原因**：我刚刚新增的“虚拟微信手机”按钮（右下角绿色图标）位置设置在了 `bottom-6 right-6`。
*   **冲突**：这正好盖住了原本也在 `bottom-6 right-6` 的“全能大脑（AI 助手）”悬浮按钮。
*   **结果**：AI 助手还在运行，但是您点不到它了。

## 修复计划

我将立即执行以下调整，确保两个核心功能并存且互不干扰：

1.  **调整虚拟手机位置**
    *   将 `MockWeChatPhone` 的按钮位置向左移动（例如移至 `right-24`），让它位于“全能大脑”按钮的左侧。
    *   这样您屏幕右下角将并排显示两个入口：
        *   👉 **右侧**：信义全能大脑（AI 总控）
        *   👈 **左侧**：微信通知模拟器（接收端）

2.  **验证功能入口**
    *   确保左侧导航栏底部的 `AI 配置中心` 链接依然可用。
    *   确保点击“全能大脑”按钮能正常唤起 AI 对话框。

3.  **保持演示架构**
    *   不引入任何数据库或后端代码。
    *   继续使用浏览器内存 (`constants.ts` / `wechatService.ts`) 来模拟数据流。

请确认执行此修复，找回您的“全能大脑”！