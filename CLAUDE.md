# 信义认证系统 V5.0 —— 接手须知

浙江信义企业管理有限公司（温州，13 人，ISO 体系认证咨询）的内部管理系统。
线上跑在腾讯云轻量 `124.223.209.102`，13 个同事每天在用 —— **它不是演示项目，改错了有人当天干不了活。**

技术栈：React 18 + Vite + TypeScript（HashRouter）/ Express + PostgreSQL 16 /
前端状态集中在 `context/AppContext.tsx`，业务表走 `server/repos/*.js`。

---

## 一、先读这个

**[docs/踩过的坑.md](docs/踩过的坑.md)** —— 这个项目已经踩过的坑，按类别整理，
每条写清「症状 / 真因 / 怎么避免」。**动手前花十分钟扫一遍，能省掉大半天。**

里面绝大多数坑的共同特征是：**不报错，但一直是错的**。
测试是绿的、页面能打开、控制台没红字，只是功能悄悄没生效 ——
这类问题靠"跑一下看看"发现不了，只能靠知道它存在。

---

## 二、不可破的规矩

这几条是业务约束，不是代码风格。违反了就是给这家公司制造真实损失。

1. **「谁做的」这条链不能断。** 任何代码路径都不许把账号连同它的记录一起删。
   经手过合同/项目/审批的账号**删不掉**是设计，不是 bug —— 那种情况用「停用」。
   见 `server/authStore.js` 的 `deleteUserIfUnused`。

2. **密码和密钥永远不进对话、不进仓库。** 放 `.env.local`（已 gitignore）。
   给用户看的密码用一次性通道，不要贴在聊天里。

3. **改数据的脚本先备份，备份失败就中止。** 部署脚本自带快照（`/opt/xinyi-releases`，留最近 8 个）。

4. **金额单位是「分」。** 库里 `project_amount = 800000` 是 8000 元。
   仓储层 `kind: 'amount'` 自动 ×100 / ÷100。**工时不要用 amount，用 `decimal`**，
   否则 0.5 小时会存成 50。

5. **改完要验「原本正常的东西还正常」**，不能只验「新功能生效了」。
   这条是血的教训：加 nginx 扫描器防护时只验了拦截生效，
   没验本站资源还能不能下载，结果样式和导出挂了 38 分钟，当时还有人在用。

---

## 三、常用命令

```bash
npm run dev          # 前后端一起起（前端 3000 / 接口 3001）
npm test             # 全量测试，需要 Docker 里的 xinyi-dev-db 在跑
npx tsc --noEmit     # 类型检查，改完必跑
bash deploy/deploy.sh   # 部署到生产（自带快照、迁移、自检）
bash deploy/rollback.sh # 回滚到上一个快照
ssh -i ~/.ssh/id_ed25519_xinyi ubuntu@124.223.209.102
```

**测试跑不起来先看 Docker**：`docker start xinyi-dev-db`。
这是本机环境问题，和线上无关 —— 报告时要说清楚，别让用户以为系统坏了。

**3000/3001 可能被用户另一个项目占用。** 要起本地服务先 `lsof -nP -iTCP:3000 -sTCP:LISTEN`
看清是谁的。**永远不要用 `pkill -f "server/app.js"` 这种模式杀进程** ——
文件名会撞车，已经误杀过用户另一个项目的开发服务器。用 PID。

---

## 四、这个项目的几个「反直觉」之处

改之前知道这些，能少走弯路：

- **权限有三份定义，不是一份。**
  `ROLE_PERMISSIONS`（能看哪些菜单）、`ROLE_CAPABILITIES`（能做哪些动作）
  都在 `constants.ts`；服务端还有一份从 `constants.ts` 解析出来的。
  只改一处的典型症状是「菜单看得见，但每个请求都 403」。

- **`checkActionPermission('X', {})` 和 `checkActionPermission('X')` 结果相反。**
  `{}` 是 truthy，会触发「这条数据是不是我的」判断，而空对象谁都不属于。
  **新建类动作不要传 context。**

- **项目分类是推出来的，不是选的。** 建项目只问两句话
  （给谁做 / 收不收钱），`projectCategory` 由 `deriveCategory()` 算。
  「有没有合同」**不是**分类依据 —— 台账指导这类活没合同也照样收钱。

- **提醒就是提醒，不许顺手建项目。** `addReminder` 曾经在关联线索/客户时
  偷偷建一个「跟进项目」，线上 15 个假项目就是这么来的。

- **前端是 HashRouter**，所有真实页面地址都是 `/`，路由在 `#` 后面。
  写 nginx 规则时要知道这一点。

---

## 五、写代码的约定

**注释写「为什么」，不写「是什么」。**
这个项目里几乎每个不显然的决定旁边都有一段注释，说明当初为什么这么选、
不这么选会怎样、以及**踩过的具体那一次**。请保持这个习惯 ——
它是这个项目能被接手的主要原因。

反例：`// 设置状态为已完成`
正例：见 `types.ts` 里 `ProjectTask.status` 那一段（为什么要有 Skipped 第三态）

**测试盯的是「会不会重犯」，不是覆盖率。**
`tests/` 里很多测试的注释就是当初的事故记录。加功能时，
如果这个功能有「悄悄失效」的可能，补一条测试把它钉住。

**给用户的文案要说清后果，不要只说不行。**
「不能删除」→「删了那些记录就变成『不知道谁做的』，这种情况用停用」。
